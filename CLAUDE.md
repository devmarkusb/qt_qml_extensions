# CLAUDE.md

Use `AGENTS.md` as the canonical repository instructions.

Claude-specific notes:

- Keep this file as a thin entry point; put shared project rules in `AGENTS.md`.
- Do not create or modify `.claude/settings.json`, `.claude/agents`, `.claude/commands`, `.claude/skills`, or `.mcp.json` unless a task identifies a concrete repository workflow that needs versioned Claude or MCP configuration.
- Follow the approval-only areas in `AGENTS.md`, especially Android signing/deployment and CI release behavior.
